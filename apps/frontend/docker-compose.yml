version: '3.8'

services:

  digital_twin_app:
    build:
      context: ./app
      dockerfile: Dockerfile
    container_name: digital_twin_app
    ports:
      - "8501:8501"
    environment:
      - BACKEND_API_URL=http://raspberrypi:8000/data
    restart: unless-stopped
